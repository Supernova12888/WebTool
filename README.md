# WebTool

## About
Using DOM in JavaScript can be pretty frustrating, confusing, and hard to use. But WebTool fixes that problem

## Installation
To install WebTool, just insert this piece of code into your HTML code. The `[VERSION]` part should be replaced with the version of WebTool you want to install. You may also replace `[VERSION]` with `latest` to get the latest version of WebTool
```html
<script src="https://cdn.jsdelivr.net/gh/Supernova12888/WebTool@[VERSION]/index.js"></script>
```

## Documentation
Take a look at WebTool's documentation by going [here](https://webtool.aquamarine.repl.co/documentation/#version-latest).

## More Information
For more information, visit WebTool's website [here](https://webtool.aquamarine.repl.co/).





